<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MilLeadershipBoard.UI.Pages.AddLessionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MilLeadershipBoard.UI.Pages"
    xmlns:vm="using:MilLeadershipBoard.UI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Page.DataContext>
        <vm:AddLessionsPageViewModel/>
    </Page.DataContext>

    <StackPanel
        Spacing="8">
        <TextBlock
            x:Uid="AddLessionsPage/TitleTextBlock"
            Style="{StaticResource TitleTextBlockStyle}"
            Margin="0,0,0,8"/>

        <TextBox
            x:Uid="AddLessionsPage/NameTextBox"
            Text="{Binding LessionName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        
        <!-- Temporary placeholder for the ComboBox -->
        <TextBox
            x:Uid="AddLessionsPage/LessionChiefComboBox"
            Text="{Binding LessionChief, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <!--<ComboBox
            x:Uid="AddLessionsPage/LessionChiefComboBox"
            IsEditable="True"
            ItemsSource="{Binding LessionChiefsItemsSource}"
            SelectedItem="{Binding LessionChief, Mode=TwoWay, TargetNullValue={Binding LessionChiefTargetNullValue}}"
            HorizontalAlignment="Stretch"/>-->
    </StackPanel>
</Page>
