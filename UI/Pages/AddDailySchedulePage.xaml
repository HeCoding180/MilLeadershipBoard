<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MilLeadershipBoard.UI.Pages.AddDailySchedulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MilLeadershipBoard.UI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    Unloaded="Page_Unloaded"
    DataContext="{x:Bind ViewModel, Mode=OneTime}">

    <StackPanel
        Spacing="8">

        <DatePicker
            x:Uid="AddDailySchedulePage/DatePicker"
            x:Name="ScheduleDatePicker"
            Date="{Binding ScheduleDateTimeOffset, Mode=TwoWay}"/>

        <Grid
            ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox
                x:Uid="AddDailySchedulePage/PathTextBox"
                Grid.Column="0"
                Text="{Binding ScheduleImagePath, Mode=TwoWay}"/>

            <Button
                x:Uid="AddDailySchedulePage/BrowseButton"
                Grid.Column="1"
                VerticalAlignment="Bottom"
                Command="{Binding BrowseCommand}"/>
        </Grid>
    </StackPanel>
</Page>
