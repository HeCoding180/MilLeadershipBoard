<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MilLeadershipBoard.UI.Pages.LocationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MilLeadershipBoard.UI.Pages"
    xmlns:config="using:MilLeadershipBoard.Config"
    xmlns:controls="using:MilLeadershipBoard.UI.Controls"
    xmlns:vm="using:MilLeadershipBoard.UI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    Unloaded="Page_Unloaded">

    <Page.DataContext>
        <vm:LocationsPageViewModel/>
    </Page.DataContext>

    <Grid
        Padding="16,8,16,16">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid
            Grid.Row="0"
            Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                x:Uid="LocationsPage_Title"
                Style="{StaticResource TitleTextBlockStyle}"/>

            <Button
                Grid.Column="1"
                Command="{Binding AddLocationCommand}"
                IsEnabled="True">

                <SymbolIcon Symbol="Add"/>
            </Button>
        </Grid>

        <controls:SequentialColumnListView
            Grid.Row="1"
            x:Name="LocationsListView"
            CanReorderItems="False"
            AllowDrop="True"
            CanDragItems="True"
            ItemTemplate="{StaticResource SoldierLocationDataTemplate}"
            ItemsSource="{x:Bind config:ConfigManager.Config.Locations, Mode=TwoWay}"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ScrollViewer.HorizontalScrollMode="Disabled"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ScrollViewer.VerticalScrollMode="Disabled"
            ScrollViewer.VerticalScrollBarVisibility="Disabled"
            Margin="-4,0"/>
    </Grid>
</Page>
